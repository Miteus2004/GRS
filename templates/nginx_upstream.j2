events {}

http {
    upstream {{ services.web.upstream_name }} {
{% for server in services.web.servers %}
        server {{ server.ip }};
{% endfor %}
    }

    server {
        listen 80;
        location / {
            proxy_pass http://{{ services.web.upstream_name }};
        }
    }
}
